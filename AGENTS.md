# Clipio — agent instructions

This project keeps a single source of truth for agent instructions.

**Read [`CLAUDE.md`](CLAUDE.md).** It covers the project goal, the development
workflow (compile-check before every handoff, batch changes, commit checkpoints),
app identity, the UI vision, and the key files in the card-shelf tree.

Do not duplicate those instructions here — this file exists only so that agents
which look for `AGENTS.md` are pointed at the right document.
